<section class="card-holder flex flex-wrap items-center justify-between gap-5">
    <slot />
</section>
<style>
    .card-holder:hover :global(.my-card:not(:hover)) {
        filter: brightness(0.85);
        transform: scale(0.95);
        opacity: 0.65;
    }
    .card-holder:hover :global(.my-card:hover) {
        transform: scale(1.025);
        box-shadow:
            0 0 1rem rgba(0, 0, 0, 0.75),
            0 1rem 2rem rgba(0, 0, 0, 0.5),
            0 2rem 4rem rgba(0, 0, 0, 0.25);
        filter: brightness(1.25);
    }
</style>